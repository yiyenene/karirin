import { NextPage } from 'next';

import { IndexPage } from "@root/components/_page/index";

const Index: NextPage<{}> = props => {
    return (
        <IndexPage />
    );
};

export default Index;
